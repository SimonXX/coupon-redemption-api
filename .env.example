# PostgreSQL container
POSTGRES_DB=coupon_redemption
POSTGRES_USER=coupon_user
POSTGRES_PASSWORD=change_me_for_local_development
POSTGRES_PORT=5433
ADMINER_PORT=8081

# Local application and migration connection string.
# Use localhost because this is meant for commands run from the host machine.
DATABASE_URL=postgresql://coupon_user:change_me_for_local_development@localhost:5433/coupon_redemption
